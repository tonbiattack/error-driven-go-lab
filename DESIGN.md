# 設計方針

Go版は、標準の`testing`パッケージだけで再現できる誤りを扱います。スライスの共有、mapのゼロ値、整数除算を題材に、言語仕様上は正しく動くが業務上の契約を破るコードをテストで観測します。

各章は、失敗するバグコミットと、同じテストを成功させる最小修正コミットを分離します。参考教材の段階的TDD手法を採用しますが、文章・コード・章名は複製しません。[1]

| 要素 | 方針 |
| --- | --- |
| テスト | Go標準の`testing` |
| 実装 | ルートパッケージの小さな関数 |
| 実行 | `go test ./...` |
| 検証 | `gofmt`、`go vet`、全テスト |

[1] [Learn Go with Tests](https://github.com/quii/learn-go-with-tests)
